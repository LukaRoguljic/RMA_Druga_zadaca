<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="@color/customColor">

    <TextView
        android:id="@+id/titleKilometers"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/tvKmToMiles"
        android:layout_centerHorizontal="true"
        android:textStyle="bold"
        android:textColor="#FF0000"
        />

    <EditText
        android:id="@+id/et_KmToMiles"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/titleKilometers"
        android:hint="@string/etKmValue"
        android:textColor="#000000"
        android:inputType="numberDecimal"
        />

    <Button
        android:id="@+id/kmConvertBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/et_KmToMiles"
        android:layout_centerHorizontal="true"
        android:text="@string/btnConvertText"
        />

</RelativeLayout>